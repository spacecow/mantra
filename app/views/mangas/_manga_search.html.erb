<%= form_tag mangas_path, :method => :get, :id => "manga_search" do %>
  <%= text_field_tag :search, params[:search], :class => "string" %>
  <%= submit_tag t('helpers.submit.search'), :title => nil, :class => "submit" %>
<% end %>
